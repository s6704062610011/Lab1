#include <stdio.h>
int main()
{
    int a=10;
    int b=15;
    if (b>a)
    {
        printf("%d greater than %d",b,a);
    }
    else
    {
        printf("%d greater than %d",a,b);
    }
    return 0;
}